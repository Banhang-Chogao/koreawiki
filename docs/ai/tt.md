# `tt`

`AGENTS.md` là tài liệu chuẩn duy nhất cho lệnh `tt`, áp dụng cho Codex, Claude, Gemini và OpenCode. Đọc phần `tt — Autonomous Topic Publisher` trong đó trước khi chạy.

Entrypoints: `./tt` hoặc `python3 scripts/tt.py`.
